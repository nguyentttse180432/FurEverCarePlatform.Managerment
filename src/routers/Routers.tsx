import MainRouter from "./MainRouter";

const Routers = () => {
  // get data from local storage
  // put user data to store
  // put token to store
  // Check token and redirect to AuthRouter
  return <MainRouter />;
};

export default Routers;
